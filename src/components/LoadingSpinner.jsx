import React from "react";

const LoadingSpinner = () => {
  return (
    <div style={{ textAlign: "center", padding: "20px" }}>
      <p>Loading...</p>
      {/* Anda bisa menambahkan animasi spinner CSS di sini */}
    </div>
  );
};

export default LoadingSpinner;
